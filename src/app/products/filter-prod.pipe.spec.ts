import { FilterProdPipe } from './filter-prod.pipe';

describe('FilterProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProdPipe();
    expect(pipe).toBeTruthy();
  });
});
